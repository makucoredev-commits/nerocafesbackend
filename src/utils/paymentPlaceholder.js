/**
 * Integrate Razorpay here later — create orders, verify signatures, capture payments.
 * For now only COD is enabled from the client.
 */
export function createRazorpayOrderPlaceholder(_payload) {
  return {
    placeholder: true,
    message: 'Integrate Razorpay here later',
  };
}
